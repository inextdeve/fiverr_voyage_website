import { AppBar, Toolbar, Typography } from "@mui/material";

const Nav = () => {
  return (
    <AppBar>
      <Toolbar>
        <Typography variant="h6" component="div">
          Voyage Website
        </Typography>
      </Toolbar>
    </AppBar>
  );
};

export default Nav;
